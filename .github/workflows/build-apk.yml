name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dorg.gradle.caching=true"
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build web app
      run: npm run build
      
    - name: List dist folder
      run: ls -la dist/
      
    - name: Create assets directory
      run: mkdir -p android/app/src/main/assets/public
      
    - name: Clear old web assets
      run: rm -rf android/app/src/main/assets/public/*
      
    - name: Copy web files to Android assets
      run: cp -r dist/* android/app/src/main/assets/public/
      
    - name: Verify copied files
      run: ls -la android/app/src/main/assets/public/
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Build Debug APK
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon -i
      
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: beiti-assistant-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk